# Модульная структура анализа переписок

## 📋 Обзор

Модуль `scripts/analysis/` содержит полностью рефакторенную систему анализа Telegram переписок. Код разбит на логические модули для улучшения читаемости, тестируемости и поддержки.

## 🏗️ Архитектура

```
scripts/analysis/
├── analyze_farma.py          # Главный скрипт (оркестратор)
├── utils/                    # Утилиты общего назначения
│   ├── db.py                 # Работа с базой данных
│   ├── formatting.py         # Форматирование сообщений
│   ├── gpt5_client.py        # OpenAI клиент и парсинг ответов
│   └── response_parser.py    # Парсинг JSON ответов
├── compression/              # Модуль сжатия диалогов
│   ├── chunking.py          # Разбиение на части по датам
│   ├── compression.py        # Сжатие отдельных чанков
│   ├── batch_processing.py  # Batch API обработка
│   ├── sliding_window.py    # Скользящее окно для анализа
│   └── compress.py          # Главная функция сжатия
├── embeddings/              # Модуль эмбеддингов
│   ├── embeddings.py        # Генерация и сохранение эмбеддингов
│   └── drilldown.py         # Drill-down к исходным сообщениям
└── extraction/              # Модуль извлечения артефактов
    ├── tasks.py             # Извлечение задач
    ├── projects.py          # Извлечение проектов
    └── grouping.py         # Группировка и дедупликация
```

## 🔄 Поток обработки

### 1. Сбор данных (`utils/db.py`)

```python
from .utils.db import get_db_connection, get_all_messages_from_chats

conn = get_db_connection(DB_PATH)
messages = get_all_messages_from_chats(conn, CHAT_IDS)
```

**Функции:**
- `get_db_connection()` - подключение к SQLite
- `get_all_messages_from_chats()` - сбор сообщений из указанных чатов
- `get_recent_contexts()` - получение недавних контекстов
- `get_messages_by_ids()` - получение сообщений по ID
- `search_messages_by_keywords()` - поиск по ключевым словам

### 2. Форматирование (`utils/formatting.py`)

```python
from .utils.formatting import format_messages_as_thread

thread_text = format_messages_as_thread(messages)
```

**Функции:**
- `format_messages_as_thread()` - форматирование в единый поток с группировкой по дням
- `get_short_name()` - получение короткого имени пользователя

### 3. Сжатие (`compression/`)

```python
from .compression import compress_thread_with_smart_model

compressed = compress_thread_with_smart_model(
    thread_text,
    max_chunk_size=10000,
    output_dir=COMPRESSED_PARTS_DIR
)
```

**Процесс:**
1. **Chunking** (`chunking.py`) - разбиение на части по датам
2. **Compression** (`compression.py`) - сжатие отдельных чанков через GPT-5
3. **Batch Processing** (`batch_processing.py`) - обработка через Batch API
4. **Sliding Window** (`sliding_window.py`) - анализ последних частей + предыдущая выжимка

**Особенности:**
- Инкрементальная обработка (только новые даты)
- Кеширование по SHA256 хешам
- Batch API для снижения стоимости на 50%
- Скользящее окно для поддержания контекста

### 4. Эмбеддинги (`embeddings/`)

```python
from .embeddings import save_embeddings_for_level, drill_down_to_raw_messages

# Сохранение эмбеддингов
save_embeddings_for_level('tasks', tasks_data, output_dir, client)

# Drill-down к исходным сообщениям
sources = drill_down_to_raw_messages(query, conn, output_dir, chat_ids)
```

**Функции:**
- `save_embeddings_for_level()` - сохранение эмбеддингов для уровня (tasks/projects/summaries)
- `drill_down_to_raw_messages()` - поиск исходных сообщений по запросу
- `find_relevant_sources_by_embedding()` - поиск через эмбеддинги
- `extract_keywords()` - извлечение ключевых слов для fallback поиска

### 5. Извлечение (`extraction/`)

```python
from .extraction import (
    extract_tasks_from_compressed_thread,
    extract_projects_with_drilldown,
    group_and_deduplicate_tasks
)

# Извлечение задач
tasks = extract_tasks_from_compressed_thread(compressed_text, client)

# Группировка и дедупликация
grouped = group_and_deduplicate_tasks(tasks, client=client)

# Извлечение проектов с drill-down
projects = extract_projects_with_drilldown(
    compressed_text,
    conn,
    output_dir,
    chat_ids,
    use_drilldown=True
)
```

**Модули:**
- **tasks.py** - извлечение задач из сжатого диалога
- **projects.py** - извлечение проектов с опциональным drill-down
- **grouping.py** - поиск похожих задач и группировка через эмбеддинги

## 📁 Структура результатов

Все результаты сохраняются в `results/farma/`:

```
results/farma/
├── compressed_parts/        # Сжатые части диалога
│   ├── cache/              # Кеш сжатых чанков (по хешам)
│   ├── parts_metadata.json # Метаданные обработанных частей
│   └── part_XX_compressed.txt
├── embeddings/              # Эмбеддинги (по уровням)
│   ├── tasks/
│   ├── projects/
│   └── summaries/
├── extracted/              # Извлеченные артефакты
│   ├── farma_tasks_extracted.json
│   ├── farma_tasks_extracted_raw.json
│   └── farma_projects_extracted.json
└── threads/                 # Исходные и сжатые потоки
    ├── farma_thread_raw.txt
    └── farma_thread_compressed.txt
```

## 🔧 Использование

### Базовый пример

```python
from scripts.analysis.analyze_farma import main

# Запуск полного анализа
main()
```

### Программное использование

```python
from pathlib import Path
from scripts.analysis.utils.db import get_db_connection, get_all_messages_from_chats
from scripts.analysis.utils.formatting import format_messages_as_thread
from scripts.analysis.compression import compress_thread_with_smart_model
from scripts.analysis.extraction import extract_tasks_from_compressed_thread

# 1. Сбор данных
conn = get_db_connection(DB_PATH)
messages = get_all_messages_from_chats(conn, CHAT_IDS)

# 2. Форматирование
thread = format_messages_as_thread(messages)

# 3. Сжатие
compressed = compress_thread_with_smart_model(thread)

# 4. Извлечение задач
tasks = extract_tasks_from_compressed_thread(compressed, client)
```

## 🎯 Преимущества модульной структуры

1. **Читаемость** - каждый модуль отвечает за одну задачу
2. **Тестируемость** - модули можно тестировать независимо
3. **Переиспользование** - функции можно использовать в других скриптах
4. **Поддержка** - легче найти и исправить баги
5. **Масштабируемость** - легко добавлять новые функции

## 📝 Модули в деталях

### `utils/` - Утилиты

**db.py** - работа с базой данных:
- Подключение к SQLite
- Запросы сообщений
- Поиск по ключевым словам

**formatting.py** - форматирование:
- Преобразование сообщений в читаемый поток
- Группировка по дням
- Оптимизация формата (удаление дубликатов)

**gpt5_client.py** - OpenAI клиент:
- Инициализация клиента с правильными параметрами
- Парсинг ответов GPT-5
- Обработка различных форматов ответов

**response_parser.py** - парсинг JSON:
- Извлечение JSON из markdown блоков
- Валидация структуры ответов

### `compression/` - Сжатие

**chunking.py** - разбиение на части:
- Разбиение по датам (стабильные границы)
- Учет максимального размера чанка
- Сохранение метаданных (даты, размеры)

**compression.py** - сжатие чанков:
- Использование GPT-5 для сжатия
- Оптимизация промптов
- Обработка ошибок

**batch_processing.py** - Batch API:
- Загрузка файлов в OpenAI
- Создание батчей
- Мониторинг статуса
- Загрузка результатов

**sliding_window.py** - скользящее окно:
- Анализ последних N частей
- Учет предыдущей выжимки
- Генерация обновленной выжимки

**compress.py** - оркестрация:
- Координация всех этапов сжатия
- Инкрементальная обработка
- Кеширование результатов
- Обработка ошибок

### `embeddings/` - Эмбеддинги

**embeddings.py** - генерация:
- Создание эмбеддингов через OpenAI
- Сохранение в файлы
- Вычисление косинусного сходства

**drilldown.py** - drill-down:
- Поиск релевантных сообщений по запросу
- Использование эмбеддингов для семантического поиска
- Fallback на keyword search
- Извлечение ключевых слов

### `extraction/` - Извлечение

**tasks.py** - извлечение задач:
- Парсинг сжатого диалога
- Извлечение структурированных задач
- Валидация результатов

**projects.py** - извлечение проектов:
- Анализ финальной выжимки
- Опциональный drill-down к исходным сообщениям
- Структурирование проектов

**grouping.py** - группировка:
- Поиск похожих задач через эмбеддинги
- Дедупликация
- Группировка дубликатов

## 🚀 Запуск анализа

### Через главный скрипт

```bash
cd tg-analyz
source .venv/bin/activate
python -m scripts.analysis.analyze_farma
```

### Настройка

Измените константы в `analyze_farma.py`:

```python
# База данных
DB_PATH = project_root / "data" / "accounts" / "ychukaev" / "messages.sqlite"

# Чаты для анализа
CHAT_IDS = {
    "Евгений Батраев": "5684787189",
    "Никита Байкалов": "8109974557", 
    "IT farmaplus24.ru": "-1002823423591"
}
```

## 🔍 Отладка

### Проверка промежуточных результатов

```python
# Проверить сжатые части
ls results/farma/compressed_parts/cache/

# Проверить эмбеддинги
ls results/farma/embeddings/tasks/

# Проверить извлеченные задачи
cat results/farma/extracted/farma_tasks_extracted.json
```

### Логирование

Все модули выводят подробные логи:
- ✅ Успешные операции
- ⚠ Предупреждения
- ❌ Ошибки
- 📊 Статистика

## 📚 Связанная документация
- [Main README](../README.md) - общая документация проекта

