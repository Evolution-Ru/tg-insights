# Синхронизация Telegram ↔ Asana

Двусторонняя синхронизация задач между анализом Telegram переписок и проектами в Asana.

## Суть

**Проблема:** Задачи из Telegram анализа существуют отдельно от Asana → дублирование, потеря контекста, неактуальность.

**Решение:** Автоматическое сопоставление через эмбеддинги (быстро и дешево), обогащение контекстом, создание недостающих задач.

## Компоненты

- `AsanaSync` - основной класс синхронизации
- `sync_farma.py` - скрипт запуска для проекта "Фарма+" (интеграция с MCP)
- `run_sync_now.py` - скрипт для тестирования (dry_run режим)

## Алгоритм

1. **Загрузка:** задачи из Telegram (JSON) + задачи из Asana (MCP API или файл)
2. **Извлечение контекста:** для каждой задачи Asana извлекаем контекстную выжимку (название, ключевые моменты, статус реализации)
3. **Сопоставление:** семантическое сравнение через эмбеддинги с двухэтапной проверкой
   - Создание эмбеддингов для всех задач Asana (на основе контекстных выжимок, батчами)
   - Создание эмбеддингов для задач Telegram
   - Для каждой задачи Telegram: поиск лучшего совпадения через косинусное сходство
   - **Этап 1:** Если score >= порога (0.75) → совпадение найдено
   - **Этап 2:** Если score между низким порогом (0.65) и основным порогом → GPT-5 проверка
   - Опциональная финальная проверка через GPT-5 для всех совпадений (по умолчанию выключена)
4. **Анализ покрытия:** расчет процента покрытия Telegram задач в Asana и статуса реализации
5. **Обогащение:** дополнение задач Asana контекстом из Telegram
6. **Создание:** создание задач в Asana для `telegram_only`
7. **Отчет:** генерация отчета с контекстными выжимками и анализом покрытия

## Использование

### Тестирование (dry_run)

```bash
cd tg-analyz
source .venv/bin/activate
python scripts/analysis/sync/run_sync_now.py
```

Логи сохраняются в `logs/sync_asana_*.log`

### Программное использование

```python
from scripts.analysis.sync.asana_sync import AsanaSync
from pathlib import Path

sync = AsanaSync()

# Загрузка задач
telegram_tasks = sync.load_telegram_tasks(Path("results/farma/extracted/farma_tasks_extracted.json"))
asana_tasks = [...]  # Загрузить через MCP или из файла

# Сопоставление с двухэтапной проверкой
matching = sync.find_matching_tasks(
    telegram_tasks,
    asana_tasks,
    similarity_threshold=0.75,  # Основной порог для совпадений
    use_embeddings=True,  # Использовать эмбеддинги (рекомендуется)
    use_gpt5_verification=False,  # GPT-5 проверка для всех совпадений (по умолчанию выключена)
    low_threshold=0.65,  # Низкий порог для потенциальных совпадений
    use_two_stage_matching=True  # Двухэтапное совпадение (по умолчанию включено)
)

# Генерация отчета
report = sync.generate_sync_report(matching, Path("results/farma/sync/sync_report.json"))
```

## Оптимизация производительности

### Эмбеддинги vs GPT-5

**Эмбеддинги (рекомендуется):**
- ⚡ Быстро: ~3-5 минут для 21×100 задач
- 💰 Дешево: ~$0.002 (почти бесплатно)
- ✅ Достаточно точно для поиска похожих задач

**GPT-5 (опционально):**
- 🐌 Медленно: ~70 минут для 21×100 задач
- 💸 Дорого: ~$20-50
- 🎯 Максимальная точность

### Двухэтапное совпадение

**Как работает:**
1. **Этап 1:** Быстрый поиск через эмбеддинги
   - Если score >= 0.75 → совпадение найдено ✅
   - Если score < 0.65 → совпадения нет ❌
   - Если 0.65 <= score < 0.75 → переход к этапу 2 ⚠️

2. **Этап 2:** GPT-5 проверка потенциальных совпадений
   - Только для задач с score между 0.65 и 0.75
   - Если GPT-5 подтверждает (score >= 0.75) → совпадение найдено ✅
   - Если GPT-5 не подтверждает → совпадения нет ❌

**Преимущества:**
- Находит больше совпадений без лишних затрат
- GPT-5 используется только для потенциальных совпадений (не для всех)
- Экономия средств: GPT-5 вызывается только когда нужно

### Рекомендации

- Используйте `use_embeddings=True` по умолчанию
- Включайте `use_two_stage_matching=True` для лучшего покрытия
- Основной порог схожести: 0.75-0.8
- Низкий порог для потенциальных совпадений: 0.65-0.70
- Включайте `use_gpt5_verification=True` только если нужна проверка всех совпадений

## Структура отчета

```json
{
  "timestamp": "2025-11-09T17:00:00",
  "summary": {
    "matched_tasks": 12,
    "telegram_only": 9,
    "asana_only": 3,
    "coverage_analysis": {
      "total_telegram_tasks": 21,
      "covered_tasks": 12,
      "coverage_percentage": 57.1,
      "status_breakdown": {
        "completed": 5,
        "in_progress": 4,
        "not_started": 3
      }
    }
  },
  "matches": [
    {
      "telegram_task": {...},
      "asana_task": {...},
      "similarity_score": 0.85,
      "asana_context": {
        "summary": "Краткая выжимка задачи",
        "key_points": ["Ключевой момент 1", "Ключевой момент 2"],
        "status": "in_progress",
        "implementation_details": ["Деталь реализации 1"]
      },
      "recommended_updates": {...}
    }
  ],
  "telegram_only": [...],
  "asana_only": [...]
}
```

## Конфигурация

```python
ASANA_PROJECT_GID = "1210655252186716"  # Фарма+
ASANA_WORKSPACE_GID = "624391999090674"
ASANA_USER_GID = "1169547205416171"
similarity_threshold = 0.75  # Основной порог совпадения
low_threshold = 0.65  # Низкий порог для потенциальных совпадений
```

## Интеграция с MCP

MCP инструменты доступны напрямую в контексте Cursor через функции типа:
- `mcp_mcp-config-el8wcq_ASANA_GET_TASKS_FROM_A_PROJECT()`
- `mcp_mcp-config-el8wcq_ASANA_CREATE_A_TASK()`
- `mcp_mcp-config-el8wcq_ASANA_UPDATE_A_TASK()`

**Прямые вызовы (рекомендуется):**
```python
result = mcp_mcp-config-el8wcq_ASANA_GET_TASKS_FROM_A_PROJECT(
    project_gid="1210655252186716",
    limit=100,
    opt_fields=["name", "notes", "completed"]
)

if result.get('successfull') or result.get('successful'):
    tasks = result.get('data', {}).get('data', [])
```

**Через обертку (опционально):**
```python
from scripts.analysis.sync.direct_mcp import create_direct_mcp_client

mcp_client = create_direct_mcp_client()
result = mcp_client.call_tool("mcp_mcp-config-el8wcq_ASANA_GET_TASKS_FROM_A_PROJECT", {...})
```

Подробнее см. `direct_mcp.py` и `sync_farma.py`.

## Что делает синхронизация

**Для совпадающих задач:**
- Добавляет описания из Telegram в `notes`
- Добавляет контекст обсуждений (чаты, темы)
- Обновляет статусы (`completed`)
- Добавляет дедлайны (`due_on`)

**Для задач только в Telegram:**
- Создает задачу в Asana
- Добавляет в проект
- Назначает ответственного
- Сохраняет весь контекст

## Тестирование

### Интеграционное тестирование

1. Запустить `run_sync_now.py` → проверить отчет в `results/farma/sync/sync_report.json`
2. Проверить логи в `logs/sync_asana_*.log`
3. Проверить качество сопоставления (score >= 0.75)
4. Проверить контекстные выжимки из Asana задач
5. Проверить анализ покрытия (процент покрытия Telegram задач в Asana)
6. Проверить рекомендации по обновлению
7. Применить изменения через `sync_farma.py` (dry_run=False)

## Статус

- [x] Базовая инфраструктура
- [x] Семантическое сравнение через эмбеддинги (быстро и дешево)
- [x] Двухэтапное совпадение (низкий порог + GPT-5 проверка)
- [x] Опциональная проверка через GPT-5
- [x] Сопоставление задач
- [x] Извлечение контекстных выжимок из задач Asana
- [x] Анализ покрытия Telegram задач в Asana
- [x] Обогащение контекстом
- [x] Генерация отчетов с контекстом и анализом покрытия
- [x] Логирование в файлы
- [x] Интеграционное тестирование (проверено качество совпадений)
- [x] Функции загрузки/обновления/создания через MCP реализованы
- [ ] Интеграционное тестирование с реальным MCP клиентом
- [ ] Автоматическое создание задач (готово, требует тестирования)
- [ ] Автоматическое обновление задач (готово, требует тестирования)
